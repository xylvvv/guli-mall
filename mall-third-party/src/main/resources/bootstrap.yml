spring:
  application:
    name: mall-third-party
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: localhost:8848 # nacos服务端地址
      config:
        file-extension: yaml # 文件后缀名
#        extension-configs:
#          - data-id: oss.yaml
#            group: DEFAULT_GROUP
#            refresh: true
#        namespace: public